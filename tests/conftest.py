"""Root conftest for the in-repo test suite.

The package is expected to be installed in editable mode (``pip install -e .``),
so ``mor`` is importable without any ``sys.path`` manipulation. This file is
kept as an anchor for shared fixtures if/when they are added.
"""
